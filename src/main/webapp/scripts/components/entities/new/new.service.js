'use strict';

angular.module('teamstudyApp')
    .factory('New', function ($resource) {
        return $resource('api/news/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
